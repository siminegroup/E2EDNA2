name: e2edna
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.8.10
  - openmm=7.7.0
  - openmmforcefields=0.11.2
  - cudatoolkit=11.7         # double check the cuda version of the Nvidia driver on your GPU. If no GPU, should comment out this line.
  - pdbfixer=1.8.1
  - mdanalysis=2.1.0
  - ambertools=22.0
  - pyyaml=6.0
  - beliveau-lab::nupack=4
  - pip=22.3.1
  - pip:                     # these packages are only distributed through PyPI
    - lightdock==0.9.2
    - seqfold==0.7.7
    - peptidebuilder==1.1.0
prefix: /home/taoliu/miniconda3/envs/e2edna  # this line is optional: where the conda environment will be created
